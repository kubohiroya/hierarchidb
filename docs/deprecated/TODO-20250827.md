# TODO リスト - 2025/08/27

## 概要
このドキュメントは、HierarchiDBプロジェクトにおける未実装機能の詳細な分析と実装計画をまとめたものです。

---

## 1. アプリケーション層の未実装機能

### 1.1 Import/Export Hook実装

#### 現状の課題
- `useImportExport` hookがコメントアウトされており、インポート/エクスポート機能が利用できない
- TreeConsoleIntegrationでインポート機能のTODOが存在

#### 本来あるべき機能
- ファイルのインポート/エクスポートをWorker API経由で実行
- 各種フォーマット（JSON, CSV等）のサポート
- プログレス表示とエラーハンドリング

#### 解決のために明らかにする必要がある点
- Import/Exportプラグインの現在の実装状況
- Worker APIでのファイル処理方法
- サポートするファイルフォーマットの仕様

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L14, L112, L344)
- `packages/feature-plugin/import-export/src/handlers/ImportExportEntityHandler.ts`

---

### 1.2 ナビゲーション機能

#### 現状の課題
- ノード選択時の遷移処理が未実装
- 前進/後退ナビゲーションが未実装

#### 本来あるべき機能
- ノードタイプに応じた適切な画面遷移
- ブラウザの履歴と連動した前進/後退
- React Routerとの統合

#### 解決のために明らかにする必要がある点
- ノードタイプごとの遷移先ルート定義
- React Router v7での履歴管理方法
- 現在位置の状態管理

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L122, L250, L255, L260)
- `app/src/routes/t.($treeId).($pageNodeId).($targetNodeId).($nodeType).tsx`

---

### 1.3 検索機能（Worker API経由）

#### 現状の課題
- 検索機能がWorker API経由で実装されていない
- N-gram索引による全文検索が未実装

#### 本来あるべき機能
- リアルタイム検索
- 全文検索とメタデータ検索
- 検索結果のハイライト表示
- 検索履歴の保存

#### 解決のために明らかにする必要がある点
- Worker APIの検索エンドポイント仕様
- インデックスの構築方法
- 検索パフォーマンスの最適化方法

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L173)
- `packages/ui/treeconsole/base/src/hooks/useTreeViewController.tsx` (L321)
- `packages/ui/treeconsole/base/src/components/TreeConsoleToolbar_Deprecated.tsx` (L162, L192)

---

### 1.4 CRUD操作（編集・削除）

#### 現状の課題
- 編集・削除機能の実装が不完全
- 編集セッション管理ロジックが未実装

#### 本来あるべき機能
- インライン編集
- バッチ削除
- 削除の確認ダイアログ
- 編集の競合検出

#### 解決のために明らかにする必要がある点
- Working Copy patternとの統合方法
- 編集状態の管理方法
- 楽観的UIの実装方法

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L194, L200)
- `packages/ui/treeconsole/base/src/hooks/useCRUDOperations.tsx` (L198, L211)

---

### 1.5 Undo/Redo機能

#### 現状の課題
- Worker API経由でのUndo/Redo実装がない
- Ring Bufferパターンとの統合が未完成

#### 本来あるべき機能
- 全操作の取り消し/やり直し
- 操作履歴の可視化
- グループ化された操作のUndo/Redo
- 永続化された履歴

#### 解決のために明らかにする必要がある点
- Command Patternの実装詳細
- Worker APIのUndo/Redoエンドポイント
- 履歴の永続化方法

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L319, L324)
- `packages/runtime/worker/src/services/CommandManager.ts` (要確認)

---

### 1.6 Copy/Paste/Duplicate機能

#### 現状の課題
- Worker API経由でのコピー&ペースト未実装
- canPasteメソッドが利用不可

#### 本来あるべき機能
- 単一/複数ノードのコピー&ペースト
- カット&ペースト
- ノードの複製
- クロスツリーでのペースト

#### 解決のために明らかにする必要がある点
- クリップボード管理の実装方法
- ノードIDの再生成ロジック
- 参照整合性の保持方法

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L329, L334, L339)
- `packages/ui/treeconsole/base/src/hooks/useCopyPasteOperations.tsx` (L95, L132, L239, L250)
- `packages/ui/treeconsole/base/src/adapters/commands/TreeMutationCommands.ts` (L181, L183)

---

### 1.7 認証関連

#### 現状の課題
- Turnstileトークンサポートが未実装
- ガイドツアー機能が未実装

#### 本来あるべき機能
- Cloudflare Turnstileによるボット対策
- 初回ユーザー向けガイドツアー
- 認証フローの可視化

#### 解決のために明らかにする必要がある点
- Turnstile APIの統合方法
- ガイドツアーのステップ定義
- 認証状態の管理方法

#### 関連ファイル
- `app/src/routes/auth.login.tsx` (L27)
- `app/src/routes/_index.tsx` (L186)

---

## 2. UIコンポーネント層の未実装機能

### 2.1 TreeConsole - 展開/折りたたみ機能

#### 現状の課題
- 全展開/全折りたたみ機能が未実装
- 展開状態の永続化が不完全

#### 本来あるべき機能
- ワンクリックでの全展開/全折りたたみ
- 展開状態の保存と復元
- 部分的な展開/折りたたみ

#### 解決のために明らかにする必要がある点
- パフォーマンスへの影響（大規模ツリー）
- 展開状態の保存先（EphemeralDB vs LocalStorage）

#### 関連ファイル
- `packages/ui/treeconsole/treetable/src/orchestrator/index.ts` (L58, L62)
- `packages/ui/treeconsole/base/src/components/TreeTable/orchestrator/ExpansionOrchestrator.ts` (L96)

---

### 2.2 TreeConsole - 編集状態管理

#### 現状の課題
- editingNodeIdの状態管理が未実装
- インライン編集のUI実装が不完全

#### 本来あるべき機能
- セル単位のインライン編集
- 編集モードの視覚的フィードバック
- ESCキーでのキャンセル
- Enterキーでの確定

#### 解決のために明らかにする必要がある点
- 編集可能なフィールドの定義
- バリデーションルール
- 編集中の他ユーザーへの通知方法

#### 関連ファイル
- `packages/ui/treeconsole/treetable/src/orchestrator/index.ts` (L71, L76)
- `packages/ui/treeconsole/treetable/src/state/index.ts` (L41)

---

### 2.3 TreeConsole - ドラッグ&ドロップ

#### 現状の課題
- dragStart, dragEnd, dropハンドラが未実装
- ドラッグ中の視覚的フィードバックなし

#### 本来あるべき機能
- ノードのドラッグによる移動
- 複数選択でのドラッグ
- ドロップ可能位置のハイライト
- 無効なドロップの防止

#### 解決のために明らかにする必要がある点
- ドラッグ可能なノードタイプ
- ドロップ可能な親子関係のルール
- タッチデバイスでの対応

#### 関連ファイル
- `packages/ui/treeconsole/treetable/src/orchestrator/index.ts` (L109, L113, L117)
- `packages/ui/treeconsole/treetable/src/state/index.ts` (L85)

---

### 2.4 認証UIコンポーネント ✅ **完了** (TDD開発完了 - 66テストケース全通過)

#### 現状の課題
- ~~AuthServiceが未実装~~ → ✅ **実装完了** (OAuth2.0ポップアップ認証)
- ~~UserAvatar, DropdownMenuがプレースホルダー~~ → ✅ **実装完了** (UI統合完了)
- ~~useAuthLibが未実装~~ → ✅ **実装完了** (認証フック統合)

#### 本来あるべき機能
- ユーザー情報の表示
- ログアウト機能
- プロフィール編集
- 権限に応じたメニュー表示

#### 解決のために明らかにする必要がある点
- 認証プロバイダーとの統合方法
- ユーザー情報の取得方法
- セッション管理方法

#### 関連ファイル
- `packages/ui/auth/src/components/AuthMethodSettings.tsx` (L20)
- `packages/ui/auth/src/components/UserAvatarMenu.tsx` (L14, L22, L27)
- `packages/ui/auth/src/contexts/MultiAuthContext.tsx` (L5, L6)

---

## 3. Worker層の未実装機能

### 3.1 TreeSubscriptionService - 深度チェック

#### 現状の課題
- 深度チェックが未実装（現在は全深度を許可）
- nodeIdからTreeIdを取得する実装がない

#### 本来あるべき機能
- サブスクリプションの深度制限
- パフォーマンス最適化
- 循環参照の検出

#### 解決のために明らかにする必要がある点
- 深度計算アルゴリズム
- パフォーマンスへの影響
- デフォルトの深度制限値

#### 関連ファイル
- `packages/runtime/worker/src/services/TreeSubscriptionService.ts` (L448, L1130)
- `packages/runtime/worker/src/services/__tests__/TreeSubscriptionService.subtree.test.ts` (L120)

---

### 3.2 NodeLifecycleManager - ハンドラー取得

#### 現状の課題
- getHandlerメソッドがレジストリに未実装

#### 本来あるべき機能
- ノードタイプ別のハンドラー取得
- フォールバックハンドラー
- ハンドラーのキャッシング

#### 解決のために明らかにする必要がある点
- ハンドラーの登録方法
- 優先順位の管理
- エラーハンドリング

#### 関連ファイル
- `packages/runtime/worker/src/lifecycle/NodeLifecycleManager.ts` (L257, L287)

---

## 4. プラグイン層の未実装機能

### 4.1 Stylerプラグイン - 完全未実装

#### 現状の課題
- **全CRUD操作が `throw new Error('Not implemented')`**
- Worker層での実装が必要

#### 本来あるべき機能
- スタイルマップの作成・読取・更新・削除
- スタイル定義の管理
- テーマの適用

#### 解決のために明らかにする必要がある点
- スタイルマップのデータ構造
- Worker APIとの統合方法
- スタイルの継承ルール

#### 関連ファイル
- `packages/plugins-plugin/styler/src/handlers/StylerEntityHandler.ts` (L258, L262, L266, L270)
- `packages/plugins-plugin/styler/src/extension/handler.ts` (L24, L38, L53)

---

### 4.2 BaseMapプラグイン - 通知システム

#### 現状の課題
- 通知システムとの統合が未実装
- 確認ダイアログの統合が未実装
- PeerEntityManagerの更新・削除メソッドが未実装

#### 本来あるべき機能
- 操作成功/失敗の通知
- 削除前の確認ダイアログ
- エラーメッセージの表示

#### 解決のために明らかにする必要がある点
- 通知システムのAPI
- ダイアログコンポーネントの仕様
- PeerEntityManagerの拡張方法

#### 関連ファイル
- `packages/plugins-plugin/basemap/backup-old-architecture/ui/plugin.ts` (L43, L52, L57, L65)
- `packages/plugins-plugin/basemap/backup-old-architecture/handlers/BaseMapEntityManager.ts` (L76, L97, L251, L267, L275)

---

### 4.3 Shapeプラグイン - ベクトルタイル生成

#### 現状の課題
- ベクトルタイル生成処理が未実装
- 座標系変換（proj4js）が未実装
- MVT形式出力が未実装

#### 本来あるべき機能
- GeoJSONからベクトルタイルへの変換
- 座標系の自動変換
- タイルキャッシュ管理
- QuadTreeによる最適化

#### 解決のために明らかにする必要がある点
- タイル生成アルゴリズム
- キャッシュ戦略
- パフォーマンス目標

#### 関連ファイル
- `docs/design/plugin-shapes/implementation-design.md` (L507, L544, L588, L602, L607, L719-L721, L733-L735)
- `packages/plugins-plugin/shape/src/services/ErrorPersistenceStrategy.ts` (L384, L510)
- `packages/plugins-plugin/shape/src/services/ErrorAggregationManager.ts` (L548, L618, L623, L675, L853)

---

## 5. 共通コア層の未実装機能

### 5.1 NodeDefinitionRegistry

#### 現状の課題
- Dexie動的スキーマ登録が未実装
- API拡張レジストリ統合が未実装

#### 本来あるべき機能
- プラグインの動的登録
- スキーマのバージョン管理
- マイグレーション処理

#### 解決のために明らかにする必要がある点
- Dexieのスキーマ更新方法
- バージョン競合の解決方法
- ロールバック戦略

#### 関連ファイル
- `packages/common/core/src/registry/NodeDefinitionRegistry.ts` (L178, L196)

---

### 5.2 型定義の未完成

#### 現状の課題
- ExpandedStateChange, SubTreeChangeの型定義が不完全
- unifiedOperationsの型定義が不適切

#### 本来あるべき機能
- 完全な型安全性
- IDE支援の向上
- ランタイムエラーの防止

#### 解決のために明らかにする必要がある点
- 実際のデータ構造
- 使用箇所の分析
- 互換性の考慮

#### 関連ファイル
- `packages/ui/treeconsole/base/src/types/index.ts` (L370, L371)
- `packages/ui/core/src/plugins/hooks/useDynamicCreateMenu.ts` (L214)

---

## 6. バックエンド層の未実装機能

### 6.1 セキュリティミドルウェア

#### 現状の課題
- アラート送信機能が未実装
- IPブロック機能が未実装

#### 本来あるべき機能
- 不正アクセスの検出と通知
- 自動IPブロック
- レート制限
- ログ記録

#### 解決のために明らかにする必要がある点
- アラート送信先（メール、Slack等）
- ブロック条件の定義
- 解除方法

#### 関連ファイル
- `packages/backend/bff/src/middleware/security.ts` (L239)

---

## 実装優先順位

### 緊急度：高
1. **Stylerプラグインの基本CRUD操作** - 完全に動作しない状態
2. **TreeConsoleのコピー&ペースト機能** - 基本的なユーザー操作
3. **検索機能** - ユーザビリティの中核機能

### 緊急度：中
4. **Undo/Redo機能** - データ操作の安全性
5. **編集状態管理** - インライン編集のUX
6. **認証UIコンポーネント** - ユーザー管理機能

### 緊急度：低
7. **ドラッグ&ドロップ** - 代替手段あり
8. **全展開/折りたたみ** - 便利機能
9. **ベクトルタイル生成** - 高度な機能
10. **セキュリティ強化** - 将来の拡張

---

## 次のステップ

1. **Stylerプラグインの実装**を最優先で着手
2. **Worker APIの拡張**（コピー&ペースト、検索）
3. **テストカバレッジの向上**
4. **ドキュメントの整備**

各機能の実装時には、以下を確認：
- [ ] TypeScript型定義の完備
- [ ] 単体テストの作成
- [ ] エラーハンドリング
- [ ] パフォーマンスへの影響評価
- [ ] ドキュメントの更新