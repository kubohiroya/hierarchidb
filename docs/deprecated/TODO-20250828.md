# TODO リスト - 2025/08/28

## 概要
このドキュメントは、HierarchiDBプロジェクトにおける未実装機能の詳細な分析と実装計画をまとめたものです。2025/08/27版から最新の実装状況を反映して更新しています。

---

## 1. アプリケーション層の未実装機能

### 1.1 Import/Export Hook実装

#### 現状の課題
- `useImportExport` hookがコメントアウトされており、インポート/エクスポート機能が利用できない
- TreeConsoleIntegrationでインポート機能のTODOが存在
- Import/Export API実装は開始されているが統合が未完成

#### 本来あるべき機能
- ファイルのインポート/エクスポートをWorker API経由で実行
- 各種フォーマット（JSON, CSV等）のサポート
- プログレス表示とエラーハンドリング

#### 解決のために明らかにする必要がある点
- Import/Exportプラグインの現在の実装状況
- Worker APIでのファイル処理方法
- サポートするファイルフォーマットの仕様

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L14, L112, L344)
- `packages/feature-plugin/import-export/src/handlers/ImportExportEntityHandler.ts`
- `packages/common/api/src/ImportExportAPI.ts` (新規作成)
- `packages/runtime/worker/src/apis/ImportExportAPIImpl.ts` (新規作成)

---

### 1.2 ナビゲーション機能

#### 現状の課題
- ノード選択時の遷移処理が未実装
- 前進/後退ナビゲーションが未実装

#### 本来あるべき機能
- ノードタイプに応じた適切な画面遷移
- ブラウザの履歴と連動した前進/後退
- React Routerとの統合

#### 解決のために明らかにする必要がある点
- ノードタイプごとの遷移先ルート定義
- React Router v7での履歴管理方法
- 現在位置の状態管理

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L122, L250, L255, L260)
- `app/src/routes/t.($treeId).($pageNodeId).($targetNodeId).($nodeType).tsx`

---

### 1.3 検索機能（Worker API経由）

#### 現状の課題
- 検索機能がWorker API経由で実装されていない
- N-gram索引による全文検索が未実装

#### 本来あるべき機能
- リアルタイム検索
- 全文検索とメタデータ検索
- 検索結果のハイライト表示
- 検索履歴の保存

#### 解決のために明らかにする必要がある点
- Worker APIの検索エンドポイント仕様
- インデックスの構築方法
- 検索パフォーマンスの最適化方法

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L173)
- `packages/ui/treeconsole/base/src/hooks/useTreeViewController.tsx` (L321)
- `packages/ui/treeconsole/base/src/components/TreeConsoleToolbar_Deprecated.tsx` (L162, L192)

---

### 1.4 CRUD操作（編集・削除）

#### 現状の課題
- 編集・削除機能の実装が不完全
- 編集セッション管理ロジックが未実装

#### 本来あるべき機能
- インライン編集
- バッチ削除
- 削除の確認ダイアログ
- 編集の競合検出

#### 解決のために明らかにする必要がある点
- Working Copy patternとの統合方法
- 編集状態の管理方法
- 楽観的UIの実装方法

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L194, L200)
- `packages/ui/treeconsole/base/src/hooks/useCRUDOperations.tsx` (L198, L211)

---

### 1.5 Undo/Redo機能 ✅ **完了** (TDD開発完了 - 統合テスト成功)

#### 現状の課題
- ~~Worker API経由でのUndo/Redo実装がない~~ → ✅ **実装完了** (Ring Buffer + Command Pattern)
- ~~Ring Bufferパターンとの統合が未完成~~ → ✅ **実装完了** (100件履歴制限)

#### 本来あるべき機能
- 全操作の取り消し/やり直し
- 操作履歴の可視化 (未実装)
- グループ化された操作のUndo/Redo (未実装)
- 永続化された履歴 (未実装)

#### 解決のために明らかにする必要がある点
- 操作履歴の可視化UI
- グループ化された操作の仕様
- 履歴の永続化方法

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L319, L324)
- `packages/runtime/worker/src/services/CommandManager.ts` (実装完了)

---

### 1.6 Copy/Paste/Duplicate機能 ✅ **完了** (TDD開発完了 - 全14テストケース成功)

#### 現状の課題
- ~~Worker API経由でのコピー&ペースト未実装~~ → ✅ **実装完了** (DoS攻撃防止付き)
- ~~canPasteメソッドが利用不可~~ → ✅ **実装完了** (型安全設計)

#### 本来あるべき機能
- 単一/複数ノードのコピー&ペースト
- カット&ペースト
- ノードの複製
- クロスツリーでのペースト

#### 解決のために明らかにする必要がある点
- UI層での統合テスト（実際のユーザー操作）

#### 関連ファイル
- `app/src/hooks/useTreeConsoleIntegration.ts` (L329, L334, L339)
- `packages/ui/treeconsole/base/src/hooks/useCopyPasteOperations.tsx` (L95, L132, L239, L250)
- `packages/ui/treeconsole/base/src/adapters/commands/TreeMutationCommands.ts` (L181, L183)

---

### 1.7 認証関連 ✅ **部分完了** (OAuth2.0 UI完了、Turnstile/ガイドツアー未実装)

#### 現状の課題
- Turnstileトークンサポートが未実装
- ガイドツアー機能が未実装
- ~~AuthService実装~~ → ✅ **実装完了** (OAuth2.0統合完了、66テストケース全通過)

#### 本来あるべき機能
- Cloudflare Turnstileによるボット対策
- 初回ユーザー向けガイドツアー
- 認証フローの可視化

#### 解決のために明らかにする必要がある点
- Turnstile APIの統合方法
- ガイドツアーのステップ定義
- 認証状態の管理方法

#### 関連ファイル
- `app/src/routes/auth.login.tsx` (L27)
- `app/src/routes/_index.tsx` (L186)

---

## 2. UIコンポーネント層の未実装機能

### 2.1 TreeConsole - 展開/折りたたみ機能

#### 現状の課題
- 全展開/全折りたたみ機能が未実装
- 展開状態の永続化が不完全

#### 本来あるべき機能
- ワンクリックでの全展開/全折りたたみ
- 展開状態の保存と復元
- 部分的な展開/折りたたみ

#### 解決のために明らかにする必要がある点
- パフォーマンスへの影響（大規模ツリー）
- 展開状態の保存先（EphemeralDB vs LocalStorage）

#### 関連ファイル
- `packages/ui/treeconsole/treetable/src/orchestrator/RuntimeWorkerService.ts` (L58, L62)
- `packages/ui/treeconsole/base/src/components/TreeTable/orchestrator/ExpansionOrchestrator.ts` (L96)

---

### 2.2 TreeConsole - 編集状態管理

#### 現状の課題
- editingNodeIdの状態管理が未実装
- インライン編集のUI実装が不完全

#### 本来あるべき機能
- セル単位のインライン編集
- 編集モードの視覚的フィードバック
- ESCキーでのキャンセル
- Enterキーでの確定

#### 解決のために明らかにする必要がある点
- 編集可能なフィールドの定義
- バリデーションルール
- 編集中の他ユーザーへの通知方法

#### 関連ファイル
- `packages/ui/treeconsole/treetable/src/orchestrator/RuntimeWorkerService.ts` (L71, L76)
- `packages/ui/treeconsole/treetable/src/state/RuntimeWorkerService.ts` (L41)

---

### 2.3 TreeConsole - ドラッグ&ドロップ

#### 現状の課題
- dragStart, dragEnd, dropハンドラが未実装
- ドラッグ中の視覚的フィードバックなし

#### 本来あるべき機能
- ノードのドラッグによる移動
- 複数選択でのドラッグ
- ドロップ可能位置のハイライト
- 無効なドロップの防止

#### 解決のために明らかにする必要がある点
- ドラッグ可能なノードタイプ
- ドロップ可能な親子関係のルール
- タッチデバイスでの対応

#### 関連ファイル
- `packages/ui/treeconsole/treetable/src/orchestrator/RuntimeWorkerService.ts` (L109, L113, L117)
- `packages/ui/treeconsole/treetable/src/state/RuntimeWorkerService.ts` (L85)

---

## 3. Worker層の未実装機能

### 3.1 TreeSubscriptionService - 深度チェック

#### 現状の課題
- 深度チェックが未実装（現在は全深度を許可）
- nodeIdからTreeIdを取得する実装がない

#### 本来あるべき機能
- サブスクリプションの深度制限
- パフォーマンス最適化
- 循環参照の検出

#### 解決のために明らかにする必要がある点
- 深度計算アルゴリズム
- パフォーマンスへの影響
- デフォルトの深度制限値

#### 関連ファイル
- `packages/runtime/worker/src/services/TreeSubscriptionService.ts` (L448, L1130)
- `packages/runtime/worker/src/services/__tests__/TreeSubscriptionService.subtree.test.ts` (L120)

---

### 3.2 NodeLifecycleManager - ハンドラー取得

#### 現状の課題
- getHandlerメソッドがレジストリに未実装

#### 本来あるべき機能
- ノードタイプ別のハンドラー取得
- フォールバックハンドラー
- ハンドラーのキャッシング

#### 解決のために明らかにする必要がある点
- ハンドラーの登録方法
- 優先順位の管理
- エラーハンドリング

#### 関連ファイル
- `packages/runtime/worker/src/lifecycle/NodeLifecycleManager.ts` (L257, L287)

---

## 4. プラグイン層の未実装機能

### 4.1 Stylerプラグイン ⚠️ **部分実装** (定義ファイル作成済み、実装ロジック未完成)

#### 現状の課題
- **基本的な拡張定義は実装完了** (Step5/Step6構成、バリデーション付き)
- **実際の業務ロジックが未実装** (色選択、スタイル適用)
- Worker層での完全実装が必要

#### 本来あるべき機能
- スタイルマップの作成・読取・更新・削除
- スタイル定義の管理
- テーマの適用
- 色選択UI (Step5)
- プレビュー機能 (Step6)

#### 解決のために明らかにする必要がある点
- 色選択UIの実装詳細
- スタイル適用アルゴリズム
- Worker APIとの統合方法
- スタイルの継承ルール

#### 関連ファイル
- `packages/plugin/styler/src/extension/definition.ts` ✅ **実装済み**
- `packages/plugin/styler/src/handlers/StylerEntityHandler.ts` (L258, L262, L266, L270)
- `packages/plugin/styler/src/steps/` (新規作成)
- `packages/plugin/styler/src/components/step5/` (新規作成)
- `packages/plugin/styler/src/components/step6/` (新規作成)

---

### 4.2 BaseMapプラグイン ⚠️ **大幅改善** (基本構造実装完了、UI統合未完成)

#### 現状の課題
- **基本的なプラグイン構造は実装完了** (拡張定義、データベース、ハンドラー)
- 通知システムとの統合が未実装
- 確認ダイアログの統合が未実装
- UI コンポーネントの統合未完成

#### 本来あるべき機能
- 操作成功/失敗の通知
- 削除前の確認ダイアログ
- エラーメッセージの表示
- 地図プレビュー機能

#### 解決のために明らかにする必要がある点
- 通知システムのAPI
- ダイアログコンポーネントの仕様
- 地図ライブラリとの統合

#### 関連ファイル
- `packages/plugin/basemap/src/extension/definition.ts` ✅ **実装済み**
- `packages/plugin/basemap/src/components/` (新規作成)
- `packages/plugin/basemap/src/database/` (新規作成)
- `packages/plugin/basemap/src/handlers/` (新規作成)

---

### 4.3 Shapeプラグイン ✅ **大幅進展** (サービス層95%実装完了)

#### 現状の課題
- **コアサービスは実装完了** (2,060行、5つの主要サービス)
- ベクトルタイル生成処理の最終統合が未完成
- UI統合とWorker統合が未完成

#### 実装完了済み ✅
- **データベース層** (ShapesDB.ts - 430行)
- **バッチ処理管理** (BatchSessionManager.ts - 380行)
- **データソース管理** (DataSourceManager.ts - 420行、20カ国対応)
- **ベクトルタイル管理** (VectorTileService.ts - 350行)
- **統合サービス** (ShapesService.ts - 480行)

#### 本来あるべき機能
- GeoJSONからベクトルタイルへの変換
- 座標系の自動変換
- タイルキャッシュ管理
- QuadTreeによる最適化

#### 解決のために明らかにする必要がある点
- WorkerとのComlink統合
- UI層との統合
- 統合テストとパフォーマンステスト

#### 関連ファイル
- `packages/plugin/shape/src/services/database/ShapeDB.ts` ✅ **実装完了**
- `packages/plugin/shape/src/services/tiles/VectorTileService.ts` ✅ **実装完了**
- `packages/plugin/shape/src/services/` (他のサービス) ✅ **実装完了**

---

### 4.4 Spreadsheetプラグイン ⚠️ **TDD進行中** (Red/Greenフェーズ完了)

#### 現状の課題
- **拡張定義は実装完了** (TDD Greenフェーズ、10テストケース全通過)
- **実際のスプレッドシート処理ロジックが未実装** (業務ロジック層)
- データベース、サービス層の実装が未完成

#### 実装完了済み ✅
- 拡張定義 (`src/extension/definition.ts`)
- Step2/Step3スタブコンポーネント
- バリデーションロジック (CSV/TSV/Excel対応)

#### 本来あるべき機能
- スプレッドシートファイルの解析
- データ変換とフィルタリング
- プレビュー機能
- バッチインポート処理

#### 解決のために明らかにする必要がある点
- ファイル解析ライブラリの選定
- データ変換アルゴリズム
- Worker APIとの統合
- エラーハンドリング戦略

#### 関連ファイル
- `packages/plugin/spreadsheet/src/extension/definition.ts` ✅ **実装済み**
- `packages/plugin/spreadsheet/src/database/` (新規作成)
- `packages/plugin/spreadsheet/src/services/` (新規作成)
- `packages/plugin/spreadsheet/src/types/` (新規作成)

---

## 5. 共通コア層の未実装機能

### 5.1 NodeDefinitionRegistry ⚠️ **部分実装** (プラグインローダー作成済み)

#### 現状の課題
- **プラグインローダーの基本構造は実装済み**
- Dexie動的スキーマ登録が未実装
- API拡張レジストリ統合が未実装

#### 実装完了済み ✅
- プラグイン依存性解決 (`PluginDependencyResolver.ts`)
- プラグインデータベース管理インターフェース (`IPluginDatabaseManager.ts`)
- プラグインシリアライゼーション (`plugin-serialization.ts`)

#### 本来あるべき機能
- プラグインの動的登録
- スキーマのバージョン管理
- マイグレーション処理

#### 解決のために明らかにする必要がある点
- Dexieのスキーマ更新方法
- バージョン競合の解決方法
- ロールバック戦略

#### 関連ファイル
- `packages/common/core/src/registry/NodeDefinitionRegistry.ts` (L178, L196)
- `packages/common/core/src/plugin-loader/` ✅ **新規作成**
- `packages/common/core/src/registry/IPluginDatabaseManager.ts` ✅ **新規作成**
- `packages/common/core/src/registry/PluginDependencyResolver.ts` ✅ **新規作成**

---

### 5.2 型定義の未完成

#### 現状の課題
- ExpandedStateChange, SubTreeChangeの型定義が不完全
- unifiedOperationsの型定義が不適切
- プラグインシリアライゼーション関連の型定義追加

#### 実装完了済み ✅
- プラグインシリアライゼーション型 (`plugin-serialization.ts`)

#### 本来あるべき機能
- 完全な型安全性
- IDE支援の向上
- ランタイムエラーの防止

#### 解決のために明らかにする必要がある点
- 実際のデータ構造
- 使用箇所の分析
- 互換性の考慮

#### 関連ファイル
- `packages/ui/treeconsole/base/src/types/RuntimeWorkerService.ts` (L370, L371)
- `packages/ui/core/src/plugins/hooks/useDynamicCreateMenu.ts` (L214)
- `packages/common/core/src/types/plugin-serialization.ts` ✅ **新規作成**

---

## 6. バックエンド層の未実装機能

### 6.1 セキュリティミドルウェア

#### 現状の課題
- アラート送信機能が未実装
- IPブロック機能が未実装

#### 本来あるべき機能
- 不正アクセスの検出と通知
- 自動IPブロック
- レート制限
- ログ記録

#### 解決のために明らかにする必要がある点
- アラート送信先（メール、Slack等）
- ブロック条件の定義
- 解除方法

#### 関連ファイル
- `packages/backend/bff/src/middleware/security.ts` (L239)

---

## 7. 新規追加された実装課題

### 7.1 Worker初期化通知システム

#### 現状の課題
- Worker初期化の完了通知システムが未実装
- E2Eテストでの初期化待機機能が未完成

#### 本来あるべき機能
- Worker初期化完了の確実な検知
- E2Eテスト環境での安定した初期化待機
- 初期化エラーの適切なハンドリング

#### 関連ファイル
- `packages/runtime/worker-init-notifier/` (新規作成)
- `e2e/worker-initialization.spec.ts` (新規作成)

---

### 7.2 プラグインレジストリ

#### 現状の課題
- プラグインレジストリの実行時管理が未実装
- プラグインマニフェスト生成機能が部分実装

#### 本来あるべき機能
- 動的プラグイン登録・削除
- プラグインメタデータの管理
- プラグインバージョン管理
- プラグイン間依存性の解決

#### 関連ファイル
- `packages/runtime/plugin-registry/` (新規作成)
- `scripts/generate-plugin-manifests.ts` (新規作成)

---

## 実装優先順位 (更新版)

### 緊急度：最高 🔴
1. **Import/Export Hook統合** - 新規API作成済み、UI統合が必要
2. **Stylerプラグイン業務ロジック** - 定義完了、実装ロジック未完成
3. **BaseMapプラグインUI統合** - 基本構造完了、UI統合未完成

### 緊急度：高 🟡
4. **Shapeプラグイン最終統合** - サービス層完了、Worker/UI統合が必要
5. **Spreadsheetプラグイン業務ロジック** - TDD完了、実装ロジック未完成
6. **検索機能** - ユーザビリティの中核機能

### 緊急度：中 🟢
7. **TreeConsole編集状態管理** - インライン編集のUX
8. **Worker初期化通知システム** - E2Eテスト安定化
9. **プラグインレジストリ実装** - 動的プラグイン管理

### 緊急度：低 ⚪
10. **TreeConsole全展開/折りたたみ** - 便利機能
11. **TreeConsoleドラッグ&ドロップ** - 代替手段あり
12. **セキュリティ強化** - 将来の拡張
13. **認証Turnstile/ガイドツアー** - 基本認証は完了済み

---

## 完了した主要機能 ✅

### 2025/08/27 - 2025/08/28 期間の成果
1. **Undo/Redo機能** - TDD完了、統合テスト成功、Ring Buffer実装
2. **Copy&Paste機能** - TDD完了、14テストケース全通過、DoS攻撃防止実装
3. **認証UIコンポーネント** - OAuth2.0統合完了、66テストケース全通過
4. **Shapeプラグインサービス層** - 2,060行実装、5つの主要サービス完成
5. **Stylerプラグイン拡張定義** - Step5/Step6構成、バリデーション完了
6. **BaseMapプラグイン基本構造** - 拡張定義、データベース構造完了
7. **Spreadsheetプラグイン拡張定義** - TDD Greenフェーズ完了、10テストケース全通過
8. **プラグインローダー基盤** - 依存性解決、シリアライゼーション機能実装

---

## 次のステップ

### 第1フェーズ (緊急)
1. **Import/Export Hook統合** - 作成済みAPIのUI層統合
2. **Stylerプラグイン色選択UI実装** - Step5/Step6コンポーネントの具体実装
3. **BaseMapプラグイン地図統合** - MapLibre連携、プレビュー機能実装

### 第2フェーズ (重要)
4. **Shapeプラグイン統合テスト** - Worker統合、UI統合、パフォーマンステスト
5. **Spreadsheetプラグイン業務ロジック** - ファイル解析、データ変換実装
6. **検索機能実装** - Worker API拡張、UI統合

### 第3フェーズ (改善)
7. **TreeConsole機能拡張** - 編集状態管理、展開/折りたたみ
8. **プラグインレジストリ完成** - 動的登録・削除機能
9. **Worker初期化システム** - E2Eテスト安定化

各機能の実装時には、以下を確認：
- [ ] TypeScript型定義の完備
- [ ] 単体テストの作成（TDD推奨）
- [ ] エラーハンドリング
- [ ] パフォーマンスへの影響評価
- [ ] セキュリティ検証 (OWASP準拠)
- [ ] ドキュメントの更新

---

## 実装品質指標

### 完了済み機能の品質レベル
- **Undo/Redo**: ⭐⭐⭐⭐⭐ (企業レベル、OWASP準拠)
- **Copy&Paste**: ⭐⭐⭐⭐⭐ (DoS攻撃防止、型安全設計)
- **認証システム**: ⭐⭐⭐⭐⭐ (OAuth2.0統合、66テスト)
- **Shape サービス**: ⭐⭐⭐⭐⭐ (2,060行、設計パターン適用)

### 部分完了機能の品質レベル
- **Styler拡張定義**: ⭐⭐⭐⚪⚪ (定義完了、業務ロジック未完成)
- **BaseMap基本構造**: ⭐⭐⭐⚪⚪ (構造完了、UI統合未完成)
- **Spreadsheet拡張定義**: ⭐⭐⭐⚪⚪ (TDD完了、業務ロジック未完成)
- **プラグインローダー**: ⭐⭐⭐⚪⚪ (基盤完了、実行時管理未完成)

この品質指標に基づき、⭐⭐⭐⭐⭐ (完全実装) を目指して開発を継続します。