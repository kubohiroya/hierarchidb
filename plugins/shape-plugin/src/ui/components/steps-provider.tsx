/**
 * The runtime plugin step registration was removed during the refactor. This file keeps
 * the module in place so imports continue to succeed while the new dialog flow is defined.
 */

export {}; // no-op placeholder
