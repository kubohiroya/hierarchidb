export const CANONICAL_YAML_ZIP_LIMITS = Object.freeze({
  archiveBytes: 16 * 1024 * 1024,
  entryCount: 12,
  entryBytes: 4 * 1024 * 1024,
  totalContentBytes: 16 * 1024 * 1024,
});

export const CANONICAL_YAML_ZIP_UTF8_FLAG = 0x0800;
export const CANONICAL_YAML_ZIP_STORE_METHOD = 0;
