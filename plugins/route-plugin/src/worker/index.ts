/**
 * Placeholder worker entry to satisfy plugin registry resolution.
 * Route plugin currently does not register additional worker stores.
 */
export const registerRouteWorkerStores = async (): Promise<void> => {
  return;
};

export default registerRouteWorkerStores;
