# L6 space fixtures

Fixtures here cover FDM space catalog and lifecycle cases. Current ready-scope fixtures keep provenance explicit when upstream data is unavailable or only advertised as unscoped capability summaries.
