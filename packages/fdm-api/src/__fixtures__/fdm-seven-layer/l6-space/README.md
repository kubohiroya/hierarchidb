# L6 space fixtures

Fixtures here cover FDM space catalog, lifecycle, and read-only cross-space placeholder cases. Current ready-scope fixtures keep provenance explicit when upstream data is unavailable or only advertised as unscoped capability summaries.

Cross-space fixtures require server-provided `origin.spaceId` for every displayed item and must not default origin from the selected dashboard space.
