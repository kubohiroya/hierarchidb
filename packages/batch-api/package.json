{
  "name": "@hierarchidb/batch-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm -w --filter @hierarchidb/batch-sdk run build:types && pnpm run build:types",
    "build:types": "api-extractor run --local"
  },
  "peerDependencies": {
    "@hierarchidb/batch-sdk": "workspace:*"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.49.3",
    "@hierarchidb/batch-sdk": "workspace:*"
  }
}
