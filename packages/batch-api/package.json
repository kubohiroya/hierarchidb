{
  "name": "@hierarchidb/batch-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm -w --filter @hierarchidb/common-types run build:types && pnpm -w --filter @hierarchidb/download run build:types && pnpm -w --filter @hierarchidb/auth-recovery run build:types && pnpm -w --filter @hierarchidb/batch-sdk run build:types && pnpm run build:types",
    "build:types": "api-extractor run --local"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.49.3"
  },
  "peerDependencies": {
  }
}
