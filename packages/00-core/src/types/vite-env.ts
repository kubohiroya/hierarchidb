/**
 * Vite environment type definitions
 * Shared across all packages to avoid duplication
 */

// Core package doesn't use Vite, so no vite/client reference needed
// This file is kept for consistency but with minimal content

// Export something to make this a module
export {};