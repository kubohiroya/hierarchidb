# @hierarchidb/util

最終更新: 2026-04-05

HierarchiDB の汎用ユーティリティパッケージ。`generateId`、`getDBName`、`DualKeyMap`、`SingletonMixin` 等、モノレポ全体で使用される共有ユーティリティを提供する。

## 依存関係

`@hierarchidb/build-api`

## ライセンス

MIT
