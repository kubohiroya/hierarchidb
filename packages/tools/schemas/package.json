{
  "name": "@hierarchidb/tools-schemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "files": [
    "plugin-manifest.schema.json"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "test": "node --test __tests__/*.test.mjs",
    "typecheck": "tsc --noEmit --project tsconfig.typecheck.json"
  },
  "devDependencies": {
    "ajv": "^8.18.0"
  }
}
