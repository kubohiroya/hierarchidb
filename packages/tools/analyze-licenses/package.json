{
  "name": "@hierarchidb/analyze-licenses",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "hdb-analyze-licenses": "dist/cli.mjs"
  },
  "exports": {
    ".": "./dist/cli.mjs",
    "./cli": "./dist/cli.mjs"
  },
  "types": "dist/cli.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node dist/cli.mjs",
    "build": "tsdown src/cli.ts --config ../../../tsdown.config.ts --splitting false --target node18 --out-extension .mjs --banner.js '#!/usr/bin/env node'",
    "clean": "rm -rf dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/license-checker": "^25.0.6",
    "license-checker": "^25.0.1"
  }
}
