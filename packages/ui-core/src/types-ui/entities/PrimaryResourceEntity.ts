import { ResourceEntity } from '../entities/ResourceEntity';

/**
 * Base interface for primary entities (configuration/metadata)
 * Primary entities store the main configuration and settings for a resource
 */
export type PrimaryResourceEntity = ResourceEntity;
