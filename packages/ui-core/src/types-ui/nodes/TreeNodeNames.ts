import { TreeNodeName } from './';

export const TreeNodeNames = {
  SuperRoot: 'SuperRoot' as TreeNodeName,
  Root: 'Root' as TreeNodeName,
  ProjectsTrash: 'ProjectsTrash' as TreeNodeName,
  ResourcesTrash: 'ResourcesTrash' as TreeNodeName,
  Projects: 'Projects' as TreeNodeName,
  Resources: 'Resources' as TreeNodeName,
} as const;
