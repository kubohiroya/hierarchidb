{
  "name": "@hierarchidb/plugin-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build":  "pnpm -w --filter @hierarchidb/plugin-sdk run build:types && pnpm run build:types",
    "build:types": "api-extractor run --local"
  },
  "peerDependencies": {
    "@hierarchidb/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.49.3",
    "@hierarchidb/plugin-sdk": "workspace:*"
  }
}
