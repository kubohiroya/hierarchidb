/**
 * Host-facing styler API contracts.
 * Styler data is stored on TreeNode data/draftData, so TreeQueryAPI/TreeMutationAPI remain the source of truth.
 */
export interface StylerQueryAPI {}

export interface StylerMutationAPI {}
