# プラグイン拡張方針（将来案）

## 目的
将来的な機能拡張に備え、UI/Icon を必須としないプラグイン設計を前提にする。

## 基本方針
- プラグインは用途に応じて UI / Worker / DB / Icon を任意に持てる。
- UI や Icon の有無を前提にした実装は避け、存在時のみロード・登録する。
- 静的 import 生成（registry 生成）でも UI/Icon の欠落を許容する。

## 想定ユースケース（例）
### 1) 認証モジュール（UI なし / Icon ありの可能性）
- Google / GitHub 等の認証連携は UI を持たず、Worker/Service 層の API 提供に限定する。
- 管理画面やメニューに表示する場合は Icon のみ提供するケースがあり得る。

### 2) タビュラーデータのデータソース拡張
- Excel 読み込みに加えて、外部 API / CSV / Parquet 等の読み込みを独立プラグイン化する。
- UI を持たず、データ取得・正規化・変換の Worker/API のみを提供する可能性がある。

### 3) データソース定義・ストラテジーのプラグイン化
- GADM / geoBoundaries などのデータソース定義や、戦略パターンの実装をプラグインに分離する。
- UI を持たない「データプロバイダ系プラグイン」を許容する。

### 4) 地図描画ライブラリの差し替え
- MapLibre GL 以外の地図ライブラリ（例: deck.gl, OpenLayers, Mapbox 等）へ切り替え可能にする。
- UI/Worker を含む「表示エンジン系プラグイン」を複数用意し、選択可能にする。

## 設計上の注意
- プラグインの有無を前提にすると UI/Worker の依存関係が壊れるため、存在チェックを必須化する。
- icon のみを持つプラグイン（UI なし）は、メニューや一覧での表示方法を別途検討する。
- pluginRegistryGeneratorPlugin などの生成物は「存在する入口だけを生成」する方針とし、未定義入口をエラー扱いにしない。

## 今後の検討事項
- UI/Icon が無いプラグインをどう UI 上で扱うか（非表示/システム用/管理専用など）
- 入口の存在チェックと、ビルド時の失敗条件をどこまで厳密にするか
- データソースや表示エンジンの選択 UI / 設定管理の設計
